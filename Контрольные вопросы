1) СКВ - это система, которая хранит все изменения проекта и позволяет вернуться позже к определенной версии файла(То есть можно отменить некоторые изменения файла, посмотреть кто из команды что-то поменял). 
2) репозиторий(git init) - папка, за которой следит Git
   коммит - фиксация состояния репозитория
   ветка - последовательность коммитов
   git merge - слияние веток
   состояние конфликта => создаем новую ветку
   git pull, git push
3) Контейнеры - корзина для переменных : гомогенные
                                         гетерогенные(pair)
- Основаны на массиве(элементы лежат строго друг за другом)

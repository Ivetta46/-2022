1)Про ГПСЧ:
Задаем зерно seed псевдослучайного генератора(например, в виде текущего времени),
потом с начальным значением выполняются определенные математические операции,
получаем какое-то число, затем с полученным числом проводятся те же мат. операции и тд.
Применяя этот алгоритм, можно получить ряд сгенерированных чисел.
 - Seed — это начальное число,с которого начинается алгоритм ГПСЧ. Для определенного начального числа мы имеем одинаковую посл-ть случайных чисел.
- генератор - источник случайности.
 распределение - закон, которому подчиняются сгенерированные числа.
 2) Итераторы - объекты, которые предоставляют функционал доступа к элементам контейнера; итератор предоставляет доступ к отдельным элементам.
 Диапазон - это пара итераторов, которые указывают начало и конец вычисления. 
Стандартный библиотечный алгоритм работает с итераторами, потому что стандартные алгоритмы уже протестированы и 
вместо того чтобы реализовывать каждый алгоритм для каждого типа контейнера, достаточно реализовать его один раз для обобщенного типа контейнера.
